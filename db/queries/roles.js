export function createRoles(roleObj){
  // destructure required column Data from roleObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

export function deleteRole(roleObj){
  // destructure required column Data from roleObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

export function findRole(param){
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}