export function savePermissions(permissionObj){
  // destructure required column Data from permissionObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

export function selectPermissions(permissionObj){
  // destructure required column Data from permissionObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

export function deletePermissions(permissionObj){
  // destructure required column Data from permissionObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

