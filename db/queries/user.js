export function saveUser(userObj){
  // destructure required column Data from userObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

export function selectUser(userDetails){
  // destructure required column Data from userObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

export function deleteUser(userId){
  // destructure required column Data from userObj
  // Run the query to store data in db tables
  // reUse promisy function that should be in utils
  // return the result
}

